name: test-workflow
namespace: default
description: test workflow
schedule: 0/2 * * * * ?
tasks:
  - name: task one
    properties:
      # command to execute
      cmd: echo
      # command args if any
      args: Hello World from task one
  - name: task two
    properties:
      # command to execute
      cmd: sleep
      # command args if any
      args: 1
    dependsOn:
      - name: task one
        mode: all
  - name: task three
    properties:
      # command to execute
      cmd: echo
      # command args if any
      args: Hello World from task three
    dependsOn:
      - name: task two
        mode: all