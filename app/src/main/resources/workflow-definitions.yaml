# task one pings google.com 4 times and runs every 5 min.
- name: workflow
  schedule: 5 0/2 * 1/1 * ? *
  description: test workflow
  tasks:
    - name: task one
      schedule: 0 0/1 * 1/1 * ? *
    - name: task two
      schedule: 0 0/1 * 1/1 * ? *
    - name: task three
      schedule: 0 0/2 * 1/1 * ? *
      dependsOn:
        - name: task two
          mode: all