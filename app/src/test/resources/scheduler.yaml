# configure namespace store
storeProviderConfig:
  providerClass: com.cognitree.kronos.scheduler.store.impl.RAMStoreProvider

timeoutPolicyConfig:
  mockPolicy:
    policyClass: com.cognitree.kronos.scheduler.policies.MockTimeoutPolicy