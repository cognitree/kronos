# name and namespace will be set while creating a workflow
description: sample workflow
tasks:
  - name: taskOne
    type: typeMockAbort
    properties:
      keyA: valA
      keyB: valB
  - name: taskTwo
    type: typeMockAbort
    properties:
      keyA: valA
      keyB: valB
    dependsOn: ["taskOne"]
